import { ModelRegistryScreen } from "@/components/lab/model-registry-screen"

export const metadata = {
  title: "Model Registry · Enexa Lab",
  description: "Versioned dispatch decision models with the frozen v1 baseline.",
}

export default function ModelsPage() {
  return <ModelRegistryScreen />
}
