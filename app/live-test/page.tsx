import { LiveTestPlanScreen } from "@/components/live-test-plan-screen"

export const metadata = {
  title: "Live Test Plan — Enexa",
  description: "Today's deliberate-scenario run sheet for on-site dispatch testing, sequenced against the day-ahead price curve.",
}

export default function LiveTestPage() {
  return <LiveTestPlanScreen />
}
